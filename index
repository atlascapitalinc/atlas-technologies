<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Atlas Technologies - Home</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&display=swap" rel="stylesheet" />
  <style>
    body {
      margin: 0;
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Helvetica, Arial, sans-serif;
      background-color: #0d1117;
      color: #c9d1d9;
      display: flex;
      height: 100vh;
      overflow: hidden;
    }

    /* Sidebar styles */
    nav.sidebar {
      background-color: #161b22;
      width: 240px;
      max-width: 240px;
      height: 100vh;
      position: fixed;
      top: 0;
      left: 0;
      border-right: 1px solid #30363d;
      padding-top: 3.5rem;
      box-sizing: border-box;
      overflow-y: auto;
      transform: translateX(0);
      transition: transform 0.3s ease;
      z-index: 1000;
    }

    nav.sidebar.closed {
      transform: translateX(-240px);
    }

    nav.sidebar ul {
      list-style: none;
      padding: 0;
      margin: 0;
    }

    nav.sidebar li {
      position: relative;
    }

    nav.sidebar > ul > li {
      border-bottom: 1px solid #30363d;
    }

    nav.sidebar a {
      display: block;
      color: #c9d1d9;
      padding: 0.75rem 1.5rem;
      text-decoration: none;
      font-weight: 300;
      font-size: 1.125rem;
      user-select: none;
      cursor: pointer;
      transition: background-color 0.2s ease;
      position: relative;
    }

    nav.sidebar a:hover {
      background-color: #238636;
      color: white;
    }

    nav.sidebar a.dropdown-toggle::after {
      content: "▸";
      position: absolute;
      right: 1.5rem;
      transition: transform 0.3s ease;
    }

    nav.sidebar li.show > a.dropdown-toggle::after {
      transform: rotate(90deg);
    }

    nav.sidebar ul ul {
      display: none;
      flex-direction: column;
      background-color: #21262d;
    }

    nav.sidebar ul ul li {
      border: none;
    }

    nav.sidebar ul ul a {
      font-size: 1rem;
      padding-left: 2.5rem;
      color: #8b949e;
      font-weight: 400;
    }

    nav.sidebar ul ul a:hover {
      background-color: #2ea043;
      color: white;
    }

    nav.sidebar li.show > ul {
      display: flex;
    }

    /* Main content */
    main.content {
      margin-left: 240px;
      padding: 2rem;
      flex: 1;
      overflow-y: auto;
      transition: margin-left 0.3s ease;
    }

    main.content.full {
      margin-left: 0;
    }

    main.content h1 {
      font-weight: 300;
      font-size: 2rem;
      color: #58a6ff;
    }

    /* Hamburger menu */
    .hamburger {
      position: fixed;
      top: 1rem;
      left: 1rem;
      width: 30px;
      height: 22px;
      cursor: pointer;
      z-index: 1100;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }

    .hamburger span {
      display: block;
      height: 4px;
      background-color: #c9d1d9;
      border-radius: 2px;
      transition: all 0.3s ease;
    }

    /* Animate hamburger when open */
    .hamburger.open span:nth-child(1) {
      transform: rotate(45deg) translate(5px, 5px);
    }
    .hamburger.open span:nth-child(2) {
      opacity: 0;
    }
    .hamburger.open span:nth-child(3) {
      transform: rotate(-45deg) translate(6px, -6px);
    }

    @media (min-width: 768px) {
      /* Always show sidebar on wider screens */
      nav.sidebar {
        transform: translateX(0);
      }
      main.content {
        margin-left: 240px;
      }
      .hamburger {
        display: none;
      }
    }
  </style>
</head>
<body>

  <div class="hamburger" id="hamburger" aria-label="Toggle menu" role="button" tabindex="0">
    <span></span>
    <span></span>
    <span></span>
  </div>

  <nav class="sidebar" id="sidebar" aria-label="Main navigation">
    <ul>
      <li>
        <a href="#" class="dropdown-toggle">Home</a>
        <ul>
          <li><a href="index.html">Overview</a></li>
          <li><a href="updates.html">Updates</a></li>
          <li><a href="team.html">Team</a></li>
        </ul>
      </li>
      <li>
        <a href="#" class="dropdown-toggle">Products</a>
        <ul>
          <li><a href="products.html#software">Software</a></li>
          <li><a href="products.html#hardware">Hardware</a></li>
          <li><a href="products.html#services">Services</a></li>
        </ul>
      </li>
      <li>
        <a href="#" class="dropdown-toggle">Solutions</a>
        <ul>
          <li><a href="solutions.html#enterprise">Enterprise</a></li>
          <li><a href="solutions.html#smallbusiness">Small Business</a></li>
          <li><a href="solutions.html#personal">Personal</a></li>
        </ul>
      </li>
      <li>
        <a href="#" class="dropdown-toggle">Services</a>
        <ul>
          <li><a href="services.html#consulting">Consulting</a></li>
          <li><a href="services.html#support">Support</a></li>
          <li><a href="services.html#training">Training</a></li>
        </ul>
      </li>
      <li>
        <a href="#" class="dropdown-toggle">Support</a>
        <ul>
          <li><a href="support.html#faq">FAQ</a></li>
          <li><a href="support.html#contact">Contact Us</a></li>
          <li><a href="support.html#resources">Resources</a></li>
        </ul>
      </li>
    </ul>
  </nav>

  <main class="content" id="mainContent">
    <h1>Welcome to Atlas Technologies</h1>
    <p>This is the homepage content area. Your main content goes here.</p>
  </main>

  <script>
    const hamburger = document.getElementById('hamburger');
    const sidebar = document.getElementById('sidebar');
    const mainContent = document.getElementById('mainContent');

    hamburger.addEventListener('click', () => {
      sidebar.classList.toggle('closed');
      hamburger.classList.toggle('open');
      mainContent.classList.toggle('full');
    });

    // Allow keyboard toggle (Enter or Space)
    hamburger.addEventListener('keydown', (e) => {
      if (e.key === 'Enter' || e.key === ' ') {
        e.preventDefault();
        hamburger.click();
      }
    });

    // Dropdown toggle for submenu
    document.querySelectorAll('.dropdown-toggle').forEach(link => {
      link.addEventListener('click', e => {
        const parentLi = e.target.parentElement;
        if (parentLi.querySelector('ul')) {
          e.preventDefault();
          parentLi.classList.toggle('show');
        }
      });
    });
  </script>

</body>
</html>
